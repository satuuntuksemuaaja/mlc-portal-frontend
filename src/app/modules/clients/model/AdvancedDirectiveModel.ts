import { MlcModel } from './common/mlc.model';

export class AdvancedDirective implements MlcModel {
  name: string;
  date: string;
  authorisedby: string;
  locationofadvanceddirective: string;
  summaryofpreferredcare: string;
  notes: string;
}
